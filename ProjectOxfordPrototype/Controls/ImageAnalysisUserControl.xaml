<UserControl
    x:Class="ProjectOxfordPrototype.Controls.ImageAnalysisUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ProjectOxfordPrototype.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    
    <Grid>
        <RelativePanel x:Name="ImageAnalysisPanel" Grid.Column="1"  Visibility="{Binding DataContext.Visibility}">
            <Image x:Name="ImageAnalysisPanelImage" Source="{Binding DataContext.Image}"></Image>
            <ListView RelativePanel.RightOf="ImageAnalysisPanelImage"></ListView>
        </RelativePanel>
    </Grid>
</UserControl>
